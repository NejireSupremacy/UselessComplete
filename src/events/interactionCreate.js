const interactionCreateE = (interaction) => {
	interaction.respondWith({
		content: 'My only use is to reply to messages that say "que" with "so" \nContact: ThisIsAName#9532',
	});
};

export { interactionCreateE };
